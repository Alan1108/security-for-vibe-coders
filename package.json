{
  "name": "security-for-vibe-coders",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter @repo/shared build && turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "turbo db:push --filter=api",
    "db:seed": "turbo db:seed --filter=api"
  },
  "devDependencies": {
    "turbo": "^2.5.4",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20"
  }
}
